# Top-level dependencies only; pip resolves compatible sub-dependencies.
# (Deliberately unpinned to avoid the bad/look-alike pins that were in here before,
#  e.g. httpx2 / httpcore2 -- those were removed. Pin exact versions later with a
#  clean `pip freeze` from a known-good venv if you want reproducible builds.)
fastapi
uvicorn[standard]
anthropic
tavily-python
python-dotenv
requests
